================================================================================
 STOCKAI — PROJECT RECAP (v4.5)
================================================================================

OVERVIEW:
  StockAI — "The database that builds itself."
  An AI-powered inventory management system with a Claude AI assistant that
  can create categories, manage items, and customize the app through natural
  language. Built on Supabase (PostgreSQL + Auth + Edge Functions + RLS).

  Includes a full web app and a production Flutter Android app with voice
  input (STT), ElevenLabs TTS, animated splash screen, and full theme support.

SUPABASE CONFIG:
  URL:      https://masngvxdbxqrrreszjxv.supabase.co
  Anon Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  (Full key in source files)
  Edge function verify_jwt: FALSE (gateway-level JWT check disabled as of v39)
  Token validation: adminDb.auth.getUser(token) inside function code (manual check)
  Note: verify_jwt: true caused all requests to 401 at gateway level (v35–v37).
        Root cause unknown (possibly Supabase gateway quirk). Manual validation
        inside function is equivalent security with no gateway interference.
  Note: JWT "invalid JWT" errors were caused by getSession() returning a stale
        cached token. Fixed in v4.0 by switching to refreshSession() in
        ai-assistant.js, which always fetches a fresh token before each request.

TECH STACK:
  Frontend:   Vanilla JS + React (Babel transpiler for inventory page)
  Backend:    Supabase (PostgreSQL, Auth, Edge Functions, RLS)
  AI:         Claude claude-haiku-4-5-20251001 via Anthropic API (tool-use loop in edge function)
              IMPORTANT: Always use claude-haiku-4-5-20251001 in smart-api-v4.ts.
              claude-haiku-4-6 does NOT exist and will cause a 404 error.
  Mobile:     Flutter (rough draft)
  Styling:    CSS Custom Properties for theming, localStorage for persistence

================================================================================
 FILE STRUCTURE
================================================================================

  landing.html              — Login/signup page (Supabase Auth) + "Learn more" link
  index.html                — Dashboard: category cards, search, sort, create, Export All
  inventory.html            — Item management (React): sort, search, CRUD, Export CSV, Import CSV
  settings.html             — Tabs: Theme | Profile | Data (export/import per category)
  about.html                — About page: branding, features, CTAs (linked from sidebar + login)
  sidebar.js                — Shared sidebar nav, auth guard, theme loader, token refresh listener
                              Note: "Inventory Manager" and 📦 legacy values auto-migrate to StockAI/SVG
  ai-assistant-v4.js        — Floating AI chat widget; full-screen overlay on mobile (≤639px)
  smart-api-v4.ts           — Edge function: Claude AI + 13 tools + direct tool_call mode
  inventory-setup-v4.sql    — SINGLE SOURCE OF TRUTH: full DB schema, RLS, triggers, seed data

  flutter_app/              — Flutter Android full-featured mobile companion
    pubspec.yaml
    lib/main.dart                          — themeNotifier, loadThemeFromSupabase()
    lib/models/theme_settings.dart         — ThemeSettings model + 6 presets
    lib/services/supabase_service.dart     — direct DB queries (categories, items, ui_settings)
    lib/services/api_service.dart          — edge function calls, refreshSession() JWT
    lib/screens/login_screen.dart          — email/password auth + signup
    lib/screens/chat_screen.dart           — AI chat, drawer, voice input, TTS, stop button
    lib/screens/settings_screen.dart       — Appearance tab + Profile tab (incl. password change)
    lib/screens/category_screen.dart       — item CRUD + CSV export

  RETIRED FILES (do not use):
    sql-setup.sql               — superseded by inventory-setup-v4.sql
    migration-fix-isolation.sql — superseded by inventory-setup-v4.sql
    smart-api-upgraded.ts       — superseded by smart-api-v4.ts
    ai-assistant.js             — superseded by ai-assistant-v4.js

================================================================================
 ARCHITECTURE
================================================================================

  AUTH FLOW:
    1. landing.html → Supabase signInWithPassword / signUp
    2. On success → redirect to index.html
    3. sidebar.js loads on every page, checks auth, redirects to landing.html if no session
    4. sidebar.js creates window.db (Supabase client), stores session/profile on window
    5. sidebar.js registers onAuthStateChange to keep window.currentSession live
    6. ai-assistant-v4.js calls window.db.auth.refreshSession() before every request
       (KEY: refreshSession() guarantees a fresh JWT; getSession() was returning stale tokens)

  SIDEBAR (sidebar.js):
    - Self-executing IIFE injected into every page via <script src="sidebar.js">
    - Creates Supabase client (window.db)
    - Auth guard: redirects to landing.html if no session
    - Registers onAuthStateChange listener → updates window.currentSession on token refresh
    - Loads user profile from profiles table
    - Loads categories from table_definitions
    - Injects fixed 260px dark sidebar on desktop, hamburger menu on mobile
    - Applies theme from localStorage (inv_theme key)
    - Dispatches 'sidebar-ready' custom event when done

  THEME SYSTEM:
    - localStorage-first: theme stored in inv_theme key as JSON
    - Inline <script> in <head> applies CSS vars BEFORE render (prevents FOUC)
    - sidebar.js also applies theme as fallback
    - 20+ CSS custom properties for colors, sizing, spacing
    - Settings page has 6 presets + custom color pickers with live preview

  MULTI-USER / DATA ISOLATION (v4.0 — hardened):
    - Supabase Auth for real user accounts
    - profiles table auto-created via trigger on auth.users INSERT
    - table_definitions: user_id is NOT NULL (DB-level enforcement, no NULL rows possible)
      UNIQUE(table_name, user_id) — two users can each have their own "toners" table
      RLS: NULL-safe policies (auth.uid() IS NOT NULL AND user_id = auth.uid())
    - field_definitions: SELECT scoped via ownership JOIN to table_definitions
      INSERT/UPDATE/DELETE locked to service role only (edge function adminDb)
    - ui_settings: user_id NOT NULL, UNIQUE(key, user_id), NULL-safe RLS
    - Dynamic item tables: NULL-safe RLS on all four operations
    - Default UI settings auto-seeded per-user on signup via DB trigger
    - Edge function fire-and-forgets seed_default_ui_settings() on every request
      as a safety net for users created before the v4.0 migration
    - sanitizeTableName() in edge function prevents SQL injection in DDL

  EDGE FUNCTION (smart-api-v4.ts) — v4.0:
    - verify_jwt: false (gateway-level check disabled; see SUPABASE CONFIG note)
    - Token validation: adminDb.auth.getUser(token) → 401 if invalid/expired
    - Two Supabase clients:
        adminDb — service role key, DDL + field_definitions writes, bypasses RLS
        userDb  — user JWT in Authorization header, all DML, RLS scopes to auth.uid()
    - assertOwnership() helper: called before every item read/write to verify the
      requesting user owns the target table via RLS-scoped userDb query
    - sanitizeTableName(): strips non [a-z0-9_] chars from table names before DDL
    - DIRECT TOOL CALL MODE: { tool_call: { name, input } } bypasses AI loop
    - AI MODE: Claude tool-use loop (up to 8 iterations)
    - 12 AI tools (run_sql removed from AI; available via direct tool_call only):
        list_categories  — userDb, RLS auto-scopes to user
        get_items        — ownership check + userDb RLS
        get_fields       — ownership check + userDb RLS (field_definitions SELECT policy
                           also enforces ownership via JOIN)
        create_category  — userDb for table_definitions, adminDb for DDL + field_definitions
        rename_category  — userDb (RLS enforces ownership)
        delete_category  — ownership check via userDb, adminDb for DROP TABLE
        add_field        — ownership check via userDb, adminDb for ALTER TABLE
        upsert_item      — ownership check + userDb, explicit user_id stamp on INSERT
        delete_item      — ownership check + userDb RLS
        get_ui_settings  — userDb, RLS scopes to user
        set_ui_setting   — userDb, upserts with (key, user_id) composite key
        set_layout       — userDb, upserts with (key, user_id) composite key
        [run_sql — adminDb, available only via direct tool_call from trusted app code]

  AI ASSISTANT (ai-assistant-v4.js):
    - Floating chat widget (bottom-right corner, ✨ button)
    - Calls window.db.auth.refreshSession() before each message (guarantees fresh JWT)
    - Falls back to getSession() if refresh fails, then to cached window.currentSession
    - Shows clear "session expired" message if no token available (instead of cryptic error)
    - Sends page context (current page, table name) with every request
    - Triggers refresh events on data changes (ai-refresh, loadInventoryCards)

  EXPORT / IMPORT:
    - inventory.html: "⬇ Export CSV" downloads current table as Google Sheets-compatible CSV
        CSV format: #fields: mapping row, display name header, then data rows
        BOM included for correct UTF-8 in Excel / Google Sheets
    - inventory.html: "⬆ Import CSV" uploads a CSV, matches columns by field name or
        display name (case-insensitive), inserts rows with user_id, reports results
    - index.html: "⬇ Export All" loops through all user categories, downloads one CSV
        per category with 400ms delay between downloads

================================================================================
 DATABASE SCHEMA
================================================================================

  profiles:
    id (uuid PK, FK auth.users ON DELETE CASCADE)
    display_name, email, role, created_at
    RLS: SELECT/UPDATE/INSERT scoped to auth.uid() = id (NULL-safe)

  table_definitions:
    id (uuid PK), table_name (text NOT NULL), display_name, icon
    user_id (uuid NOT NULL, FK auth.users ON DELETE CASCADE)
    created_at
    UNIQUE (table_name, user_id)
    RLS: NULL-safe SELECT/INSERT/UPDATE/DELETE scoped to auth.uid() = user_id

  field_definitions:
    id (uuid PK), table_name (FK → table_definitions), field_name,
    display_name, field_type, required, sort_order, created_at
    UNIQUE (table_name, field_name)
    RLS SELECT: EXISTS (SELECT 1 FROM table_definitions WHERE table_name = ... AND user_id = auth.uid())
    RLS INSERT/UPDATE/DELETE: service role only (WITH CHECK (false) for user role)

  ui_settings:
    id (uuid PK), key (text NOT NULL), user_id (uuid NOT NULL, FK auth.users ON DELETE CASCADE)
    value, updated_at
    UNIQUE (key, user_id)
    RLS: NULL-safe SELECT/INSERT/UPDATE/DELETE scoped to auth.uid() = user_id

  Dynamic item tables (e.g. toners, monitors):
    id (uuid PK DEFAULT gen_random_uuid())
    user_id (uuid NOT NULL)  — no DEFAULT auth.uid() to avoid silent null inserts
    created_at (timestamptz DEFAULT now())
    ...custom fields (text / numeric / date)
    RLS: NULL-safe per-user policies on SELECT/INSERT/UPDATE/DELETE

  Helper functions:
    exec_sql(sql text) → jsonb
      SECURITY DEFINER, EXECUTE granted to service_role only
      Used by edge function adminDb for all DDL (CREATE/DROP/ALTER TABLE, policies)

    seed_default_ui_settings(target_user_id uuid) → void
      SECURITY DEFINER, EXECUTE granted to service_role only
      Inserts 26 default ui_settings rows for a user (ON CONFLICT DO NOTHING)
      Called by on_auth_user_created trigger and fire-and-forget in edge function

  Triggers:
    on_auth_user_created → handle_new_user()
      Fires AFTER INSERT ON auth.users
      Creates profile row + seeds default UI settings for every new signup

================================================================================
 DEPLOY / SETUP STEPS
================================================================================

  Fresh install:
    1. Run inventory-setup-v4.sql in Supabase SQL Editor
       (Creates all tables, RLS, triggers, helper functions, seeds defaults)
    2. Deploy smart-api-v4.ts as edge function named "smart-api"
       - Add secret: ANTHROPIC_API_KEY = sk-ant-...
       - SUPABASE_SERVICE_ROLE_KEY is auto-injected by Supabase
       - Set JWT enforcement: OFF (verify_jwt: false — function validates manually)
    3. Host HTML/JS files on any static host (Supabase Storage, Netlify, etc.)
       Make sure to use ai-assistant-v4.js (not the old ai-assistant.js)
    4. For Flutter: open flutter_app/ in Android Studio, run flutter pub get

  Re-deploy edge function:
    Supabase Dashboard → Edge Functions → smart-api → Edit → paste → Deploy

  Migrating from v3.x:
    1. Run inventory-setup-v4.sql (safe to run on existing DB — uses IF NOT EXISTS,
       DROP POLICY IF EXISTS, ON CONFLICT DO NOTHING throughout)
       The migration block at the bottom claims any orphaned data for yjturetsky@gmail.com
    2. Swap smart-api-upgraded.ts → smart-api-v4.ts (redeploy edge function)
    3. Swap ai-assistant.js → ai-assistant-v4.js in all HTML pages
    4. Set JWT enforcement: ON in Supabase Dashboard → Edge Functions → smart-api

  Token / auth troubleshooting:
    - "Invalid JWT" errors: caused by getSession() returning a cached stale token.
      Fixed in v4.0: ai-assistant-v4.js uses refreshSession() instead.
    - If users see "session expired" messages, ensure sidebar.js onAuthStateChange
      listener is registering correctly and window.db is initialized before
      ai-assistant-v4.js runs.
    - verify_jwt: false is the correct setting (v39+). The function validates tokens
      manually via adminDb.auth.getUser(token). Setting verify_jwt: true caused
      gateway-level 401s for all requests regardless of token validity.
    - Flutter "AuthApiException: Invalid Refresh Token": user's refresh token was
      invalidated. Fixed by SessionExpiredException + _freshToken() in api_service.dart
      which signs out gracefully and navigates to LoginScreen.

================================================================================
 KNOWN ISSUES / PENDING
================================================================================

  - login.html (old file) uses hardcoded credentials — not connected to Supabase Auth.
    Use landing.html instead for real authentication.

================================================================================
 RECENT CHANGES (2026-04-14) — v4.2 StockAI Tasks Complete
================================================================================

  BRANDING (Tasks 1–2):
    - Renamed: "Inventory Manager" → "StockAI" across all files
    - New SVG cube logo replaces 📦 emoji everywhere (web + Flutter)
    - Slogan: "The database that builds itself."
    - Flutter: animated splash screen (fade+scale, 2s, navigates by session state)
    - Web: sidebar auto-migrates legacy "Inventory Manager" / 📦 to StockAI/SVG
    - "Professional Blue" preset replaces "Ocean Blue"; corrected hex values
    - Dashboard cards: border removed, replaced with box-shadow elevation
    - Theme auto-saves on color change (no Save button)

  CATEGORY & INVENTORY LOGIC (Task 3):
    - Category screen rewritten: "Required Fields" section (first 2 locked, rename-only)
    - "Custom Fields" section for all additional fields (edit + delete)
    - Required fields shown with purple chip and asterisk in item form
    - Star/title field logic removed

  ELEVENLABS TTS (Tasks 4 + voice fix):
    - Switched to /stream endpoint with eleven_flash_v2_5 model
    - stability and similarity_boost sliders added to settings (persisted to Supabase)
    - Voice list updated: all 12 current ElevenLabs premade voices (tiers=[])
    - Default voice: Sarah (EXAVITQu4vr4xnSDxMaL) — confirmed 200 on free plan
    - Old Rachel ID (21m00Tcm4TlvDq8ikWAM) removed — library voice, 402 on free tier

  WEB OPTIMIZATION (Task 5):
    - Settings tabs reordered: Theme → Profile → Data (was Profile → Theme → Categories)
    - Data tab: per-category Export CSV button + Import section (file picker + dropdown)
    - ai-assistant-v4.js: full-screen overlay on mobile ≤639px, chevron-down close button
    - web/about.html created: branding, feature grid, CTAs
    - Sidebar: "ℹ️ About" nav link added; landing page: "Learn more →" link
    - body.loaded fix: settings.html now properly fades in on ready

  BACKEND RECYCLING BIN (Task 6):
    - recycling_bin table: id, user_id, source_table, item_id, item_data (jsonb), deleted_at
    - RLS: SELECT/INSERT/DELETE per-user, index on (user_id, deleted_at)
    - delete_item in smart-api-v4.ts: fetches row → inserts to recycling_bin → purges >30d → hard-deletes
    - SQL deployed via supabase db query --linked; edge function deployed as v40

  DEPLOYMENTS:
    - Flutter APK v1.1.2 (4), 55.3MB — Firebase App Distribution, tester: Yjturetsky@gmail.com
    - Web: Netlify https://cchi.netlify.app (8 files uploaded)
    - Supabase Edge Function: smart-api v40 (ACTIVE)
    - GitHub: pushed to master

================================================================================
 RECENT CHANGES (2026-04-16) — v4.5
================================================================================

  FLUTTER — "New Category" button now bypasses AI (category creation reliability):
    - Root cause: AI (claude-haiku-4-5-20251001) was creating categories with only
      one field (quantity) and no name field, or putting a number field at position 0.
      The "Add Category" button in the chat drawer and settings Data tab sent a natural
      language prompt to the AI, which would then call create_category unreliably.
    - Fix: ApiService.createCategory(displayName, icon) added — calls create_category
      via direct tool_call mode with hardcoded fields:
          fields[0]: { field_name: "name", display_name: "Name", field_type: "text", required: true }
          fields[1]: { field_name: "quantity", display_name: "Quantity", field_type: "number" }
      table_name derived from displayName (lowercase, spaces→underscores, strip non-alphanumeric).
    - Both call sites updated:
        chat_screen.dart _NewCategorySheet.onCreated → _apiService.createCategory()
        settings_screen.dart _DataCategorySheet.onCreated → _apiService.createCategory()
    - AI chat path unchanged: typing "create a printers category with brand and model"
      still goes through the AI tool-use loop. AI can still customise field display names
      (e.g. "Printer Model") and add extra fields. Backend enforcement is the safety net.

  BACKEND — create_category enforcement (supabase/functions/smart-api/index.ts):
    - Backend now auto-corrects malformed field arrays before saving:
        If fields[0].field_type !== 'text' → prepend { name, Name, text, required:true }
        If fields.length < 2 → append { quantity, Quantity, number }
    - Tool description updated with concrete field examples and MANDATORY warning.
    - System prompt CATEGORY CREATION RULES section added with ⚠️ MANDATORY header,
      explicit field structure with examples, "A category with only one field is BROKEN".
    - Quick-reference create_category line updated: now shows both required fields.
    - CRITICAL section clarified: applies to data insertion only, not category creation.
    - NOTE: All previous edits were accidentally made to backend/smart-api-v4.ts (not
      deployed). The deployed file is supabase/functions/smart-api/index.ts — fixed.

  FLUTTER — category_screen.dart stability fixes:
    - Item form field labels: now use actual display_name (e.g. "Printer Model *")
      instead of hardcoded "Name *" / "Quantity *". Required fields still marked with *.
    - Overflow crash fixed: all showModalBottomSheet calls now wrap content in
      ConstrainedBox(maxHeight: 90% screen height) at the call site. Also added
      ConstrainedBox inside _ItemForm itself. Fixes "RenderFlex overflowed by 99231px"
      on Flutter web, especially at high browser zoom levels.
    - _submit() finally block removed: no longer calls setState after Navigator.pop.
      Prevented "Looking up a deactivated widget's ancestor" and cascade errors.
      On success: pop + return. On error: setState(saving=false) + snackbar.
    - _reload() now uses addPostFrameCallback: setState deferred to after current frame,
      preventing "Tried to build dirty widget in wrong build scope" assertion.
    - initState now sets _dataFuture = _loadData() directly (not via _reload) to
      prevent LateInitializationError on first frame before callback fires.

  FLUTTER — chat_screen.dart: categories auto-refresh after every AI response:
    - _sendMessage() now calls _loadCategories() alongside loadThemeFromSupabase()
      after every AI response. Previously the drawer category list only updated when
      the user manually opened/closed the drawer after an AI create_category call.

  DATABASE — test category cleanup + sort order fixes:
    - Deleted: test_category_1, test_category_2, test_category_3 (tables + metadata)
    - Fixed sort_orders on broken AI-created categories: name field set to sort_order=0,
      quantity to sort_order=1 so card title and stock badge render correctly.
    - Added missing 'name' text field to: desktops, phone, test, test_2 categories.

  DEPLOYMENTS:
    - Supabase Edge Function: smart-api redeployed (create_category enforcement + prompts)
    - Flutter APK v1.1.2 (4), 55.4MB — Firebase App Distribution, tester: Yjturetsky@gmail.com

================================================================================
 RECENT CHANGES (2026-04-14, session 2) — v4.3
================================================================================

  FLUTTER — Drawer double-icon fix (chat_screen.dart):
    - Categories stored in DB have display_name = "📚 Books" AND icon = "📚".
      The ListTile was showing the emoji in leading AND at the start of title text.
    - Fix: strip leading emoji + whitespace from display_name using regex
      RegExp(r'^\p{Emoji_Presentation}\s*', unicode: true) before rendering title.
    - Applied in both the main drawer ListView and the Manage Categories sheet.

  FLUTTER — Email confirmation flow (login_screen.dart):
    - signUp() previously always navigated to ChatScreen after signup, even when
      email confirmation is required (session would be null, causing crashes).
    - Fix: capture AuthResponse from signUp(); if response.session != null →
      navigate to ChatScreen (auto-confirm path); if null → show confirmation screen.
    - Added _confirmationSent bool + _pendingEmail string state fields.
    - Confirmation screen: envelope icon, "Check your email" heading, email address,
      "Back to Sign In" button. Works with Supabase "Confirm email" setting ON.

  AI — Field ordering enforcement (smart-api-v4.ts):
    - Root cause of wrong data layout: AI was creating categories with price/type
      fields at position 0 instead of item name (e.g. "Cost per Unit" as field[0]).
      Card display convention requires field[0] = identity, field[1] = quantity.
    - create_category tool description now explicitly mandates:
        fields[0]: item name/identity (text, required: true) — shown as card title
        fields[1]: quantity (number) — shown as stock count badge
        fields[2+]: any additional custom fields
      NEVER put price, cost, type, unit at position 0.
    - sort_order auto-assignment bug fixed: was i+1 (1-based), now i (0-based) to
      match system prompt convention that says "sort_order 0 = identity".
    - RULES section split into CATEGORY CREATION RULES + DATA INSERTION RULES with
      explicit "call get_fields before upsert_item", "one upsert per item" rules.
    - Deployed: supabase functions deploy smart-api --no-verify-jwt

  SECURITY — Bot account cleanup:
    - Detected bot signup: benraker80@gmail.com (created 2026-03-02, never confirmed
      email, never signed in, zero real data beyond auto-created profile row).
    - Deleted via: DELETE FROM auth.users WHERE email = 'benraker80@gmail.com'
    - Profile row cascade-deleted automatically (FK ON DELETE CASCADE).
    - Root cause: email confirmation was already enabled in Supabase but the bot
      signed up during a window when it may have been off. Confirmed it is ON now.

  SECURITY — Supabase keys removed from source code (web):
    - Anon key and URL were hardcoded in 6 web files. Moved to window.SUPABASE_URL
      and window.SUPABASE_ANON_KEY with hardcoded fallbacks for local dev.
    - New web/config.js (gitignored): sets window.SUPABASE_URL and ANON_KEY.
    - netlify.toml build command: generates config.js from Netlify env vars at
      deploy time (printf into web/config.js).
    - Added <script src="/config.js"> before other scripts in all 4 HTML pages.
    - Netlify env vars set: SUPABASE_URL, SUPABASE_ANON_KEY.
    - web/config.js added to .gitignore. Keys remain as fallbacks in JS for local
      development without the Netlify build step.
    - Files updated: sidebar.js, landing.html, ai-assistant-v4.js, index.html,
      inventory.html, settings.html.

  DEPLOYMENTS:
    - Supabase Edge Function: smart-api (AI field ordering rules)
    - Web: Netlify https://cchi.netlify.app — config.js build injection active
    - Git: committed as v4.3 (push pending GitHub re-auth)

================================================================================
 RECENT CHANGES (2026-04-13, session 2)
================================================================================

  401 API error root cause + fix:
    - A previous AI (security AI) deployed edge function v35 with verify_jwt: true
      at the Supabase gateway level. For an unknown reason the Supabase gateway was
      rejecting valid user JWTs before the function code even ran (62–179ms response
      time vs 338–843ms for working requests). Pattern: v31/v33/v34 all 200s;
      v32/v35/v36/v37 all 401s.
    - Fix: deployed v38/v39 with verify_jwt: false. The function itself validates
      tokens via adminDb.auth.getUser(token) — no security loss, gateway just no
      longer blocks before function code runs. Confirmed fix by debug curl returning
      custom "Missing Authorization header" message (proving function code executing).

  Edge function — run_sql security decision:
    - run_sql removed from Claude's AI tools array (service role bypasses RLS,
      prompt injection risk). Comment in tools: "run_sql intentionally excluded —
      service role bypasses RLS. Available via direct tool_call from trusted app
      code only."
    - run_sql case handler kept in runTool() switch for direct tool_call mode
      (used by app's deleteAccount, deleteCategory). Current deployed version: v39.

  RLS policies reverted (security AI changes undone):
    - Security AI had added TO authenticated to all policy role clauses and wrapped
      auth.uid() in (select auth.uid()). This broke queries.
    - Reverted: re-executed SQL to drop and recreate all policies on profiles,
      table_definitions, field_definitions, ui_settings without TO authenticated
      and without (select auth.uid()) wrapper. Also ran PL/pgSQL loop to revert
      all dynamic user item table policies.

  Flutter — session expiry handling (AuthApiException fix):
    - app/lib/services/api_service.dart: added _freshToken() helper that catches
      AuthApiException(refresh_token_not_found), signs out, and throws
      SessionExpiredException. All API methods use _freshToken() instead of inline
      refreshSession() calls.
    - New SessionExpiredException class at bottom of api_service.dart.
    - app/lib/screens/chat_screen.dart: _sendMessage() catch block detects
      SessionExpiredException and navigates to LoginScreen via pushAndRemoveUntil.
    - Root cause: user's refresh token was invalidated by security AI session changes.

  Flutter — status bar white bar fix:
    - app/lib/main.dart: added AppBarTheme with SystemUiOverlayStyle to both
      light and dark _buildTheme() branches.
      Light: statusBarColor: transparent, statusBarIconBrightness: Brightness.dark
      Dark:  statusBarColor: transparent, statusBarIconBrightness: Brightness.light
    - Previously status bar showed a white/system-default bar not matching theme.

  Web — dark mode AI chat text fix:
    - web/ai-assistant-v4.js: .ai-msg color changed from var(--clr-text, #333)
      to var(--clr-text-primary, var(--clr-text, #333)).
    - Root cause: Dark Mode preset defines --clr-text-primary: #f1f5f9 but not
      --clr-text, so dark text (#333 fallback) appeared on dark background.

  ElevenLabs TTS integration:
    - app/lib/services/api_service.dart: added synthesizeSpeech() method calling
      ElevenLabs API (api.elevenlabs.io/v1/text-to-speech/{voiceId}).
      Returns audio bytes. Hardcoded voice ID default: 21m00Tcm4TlvDq8ikWAM (Rachel).
    - pubspec.yaml: replaced flutter_tts with audioplayers ^6.0.0 for audio playback.
    - chat_screen.dart: _ttsVoiceId field added; settings allow voice selection.

  APK: v1.1.2 (4) — distributed to yjturetsky@gmail.com via Firebase App Distribution.
  Web: deployed to cchi.netlify.app via netlify deploy --prod.

================================================================================
 RECENT CHANGES (2026-04-13)
================================================================================

  Security hardening — RLS + edge function (NOTE: most of these were later reverted
  in session 2 above because they broke API authentication):

  backend/smart-api-v4.ts:
    - Removed run_sql from Claude's tools array. The AI no longer has arbitrary
      SQL access. The case handler remains for trusted direct tool_call clients
      but the stronger user_id check (requires WHERE user_id = 'exact-uuid',
      not just the word "user_id") prevents cross-user data access.
    - Fixed update_updated_at_column() search_path: SET search_path = '' and
      switched to pg_catalog.now() (applied via Supabase migration).
    - All RLS policies migrated TO authenticated (from TO public) and auth.uid()
      calls wrapped in (select auth.uid()) to fix per-row re-evaluation (initplan).
      [REVERTED in session 2 — broke API auth; plain auth.uid() restored]

  app/lib/services/api_service.dart:
    - deleteAccount() rewritten to call delete_user_account() RPC directly via
      Supabase SDK. The old run_sql approach was already broken (auth. schema is
      blocked by the forbidden list). RPC call is cleaner and correct.

================================================================================
 RECENT CHANGES (2026-03-02)
================================================================================

  settings.html, index.html, inventory.html, sidebar.js — Sidebar now changes
  color with theme:
    - Each preset now defines matching sidebar colors (sidebarBg, sidebarHeader,
      sidebarText, sidebarTitle) derived from the preset's palette.
    - applyPreset() now applies those 4 sidebar CSS vars live and saves them.
    - Inline <head> scripts in all 3 HTML pages now include sidebar_* keys so
      the correct sidebar colors load from localStorage immediately on every page.
    - sidebar.js colorMap already included sidebar_* keys, applied before render.
    - Color pickers in Settings → sidebar section remain fully functional for
      per-color overrides beyond the presets.

  sidebar.js — Fixed duplicate logo in sidebar header:
    - Previously, `buildSidebar()` header could show TWO box/icon shapes
      (one emoji in the title text + one from another source).
    - Fix: extracted leading emoji from app_name into window.appLogo;
      sidebar header now renders either:
        (a) <div class="sidebar-logo">{emoji}</div> + <div class="sidebar-title">{text}</div>
        (b) <div class="sidebar-title">{text}</div>  — if app_name has no leading emoji
      Guarantees exactly ONE logo element (or none), never two.

================================================================================
 RECENT CHANGES (2026-03-02, session 2)
================================================================================

  sidebar.js — Contrast + hardcoded nav color fixes:
    - nav-item hover color: hardcoded #cbd5e1 → var(--clr-sidebar-title)
    - nav-item.active background: rgba(102,126,234,0.15) → rgba(255,255,255,0.12)
      (theme-neutral; old value produced a purple blob on non-purple presets)
    - nav-section color: hardcoded #64748b → var(--clr-sidebar-text) + opacity: 0.65
    - user-email color: hardcoded #64748b → var(--clr-sidebar-text) + opacity: 0.75
    - hamburger-btn color: hardcoded #f1f5f9 → var(--clr-sidebar-title)
    - Dark Mode preset sidebarText fixed: #64748b → #94a3b8 (was below WCAG AA 4.5:1
      contrast ratio on the #0f172a background)

  sidebar.js — Font size slider now works (rem units throughout):
    - Added --font-base: 15px to :root defaults in injected CSS
    - Added html { font-size: var(--font-base); } to injected CSS so rem units
      cascade from the slider value across all pages
    - Converted ALL sidebar font sizes from px to rem:
        sidebar-title: 16px → 1.07rem
        nav-item: 14px → 0.93rem
        nav-icon: 18px → 1.2rem
        nav-section: 11px → 0.73rem
        user-avatar: 12px → 0.8rem
        user-name: 13px → 0.87rem
        user-email: 11px → 0.73rem
        sidebar-logout-btn: 18px → 1.2rem
        hamburger-btn: 20px → 1.33rem
        mobile-title: 16px → 1.07rem

  index.html, inventory.html — Font slider now scales main page content:
    - Added/updated html { font-size: var(--font-base, 16px); } in page <style>
    - Converted all major content element font-sizes from px to rem:
        Page titles, subtitles, buttons, search/sort controls, inventory cards
        (card icon, title, description, stats), form labels/inputs, modal headings,
        item field labels/values, stats bar numbers
    - Rem units mean all text scales proportionally when the font slider moves

  ai-assistant-v4.js — AI chatbox now fully themed:
    - Remaining hardcoded colors replaced with CSS custom property equivalents:
        #ai-chat-panel background: #fff → var(--clr-card, #fff)
        #ai-chat-messages background: #f8f8fc → var(--clr-bg, #f8f8fc)
        .ai-msg background: #fff → var(--clr-card, #fff)
        .ai-msg color: #333 → var(--clr-text, #333)
        .typing-indicator background: #fff → var(--clr-card, #fff)
        #ai-chat-input-area background: white → var(--clr-card, white)
        #ai-chat-input-area border-top: #eee → rgba(0,0,0,0.1)
        #ai-chat-input border: #e0e0e0 → rgba(0,0,0,0.12)
        scrollbar thumb: #ddd → rgba(0,0,0,0.15)
    - Combined with previous session's gradient/accent replacements, the full
      chatbox (button, header, message bubbles, input, background) now adapts
      to any active theme preset

================================================================================
 RECENT CHANGES (2026-03-02, Flutter expansion)
================================================================================

  Flutter app rebuilt from a 2-screen rough draft into a full mobile companion:

  New packages added (pubspec.yaml):
    speech_to_text ^7.0.0, flutter_tts ^4.0.0, share_plus ^10.0.0
  Android permission added: RECORD_AUDIO

  lib/main.dart:
    - Global themeNotifier (ValueNotifier<ThemeSettings>)
    - loadThemeFromSupabase() reads ui_settings and updates notifier live
    - MaterialApp wrapped in ValueListenableBuilder for instant theme switching
    - _buildTheme() helper: lerps all 6 Material 3 surface layers toward seed
      color (3-12%) in light mode so chosen palette is actually visible

  lib/models/theme_settings.dart (NEW):
    - ThemeSettings class (seedColor + ThemeMode)
    - kThemePresets: 6 presets matching web (Purple, Ocean, Forest, Sunset, Rose, Slate)

  lib/services/supabase_service.dart (NEW):
    - getCategories, getFieldDefinitions, getItems, upsertItem, deleteItem
    - getUiSettings, setUiSetting (handles jsonb quoted-string values)

  lib/services/api_service.dart:
    - Fixed stale JWT: refreshSession() instead of currentSession
    - Added concise: bool param — voice sends prepend "Reply in 1-2 sentences max."

  lib/screens/chat_screen.dart:
    - Navigation drawer: category list + app name/logo parsed from ui_settings
      (leading emoji extracted same pattern as sidebar.js)
    - Mic button: speech-to-text, auto-sends on final result (voice path only)
    - TTS: AI speaks back only on voice-triggered responses (silent for typed)
    - Stop button: replaces send button while speaking (red, Icons.stop_rounded)
      tracked via flutter_tts start/completion/cancel handlers
    - New Category sheet: emoji picker + name field, creates via AI (edge function)
    - loadThemeFromSupabase() called after every AI response so AI-driven theme
      changes (set_ui_setting) apply immediately without restart
    - maxLines: 5 so Enter key submits reliably

  lib/screens/settings_screen.dart (NEW):
    - Appearance tab: Light/Dark/System segmented toggle + 6 color preset grid
      changes apply live via themeNotifier + saved to ui_settings
    - Profile tab: display name editor, email (read-only), sign out
    - Password change section: new + confirm fields with show/hide toggle,
      validates min 6 chars and match, calls updateUser(UserAttributes(password:))

  lib/screens/category_screen.dart (NEW):
    - Lists items for any category using dynamic field_definitions
    - Add/edit items via bottom sheet form (field types: text, number, textarea)
    - Delete with confirm dialog
    - Export CSV via native share sheet (share_plus)

================================================================================
 RECENT CHANGES (2026-03-02, Flutter fixes)
================================================================================

  Fixed 4 Flutter issues:

  lib/services/supabase_service.dart:
    - Fix #11: getFieldDefinitions() used .order('field_order') which does not
      exist. Corrected to .order('sort_order') — category screen no longer crashes.

  lib/services/api_service.dart:
    - Fix #10: sendMessage() now accepts List<Map<String,String>> history param.
      When history is non-empty, prepends "[Previous conversation:\n...]" block
      identical to the web client (ai-assistant-v4.js). Chat now has memory
      between turns in the same session.
    - New deleteCategory(String tableName) method — sends a natural-language
      delete instruction to the edge function; used by Manage Categories sheet.

  lib/screens/chat_screen.dart:
    - Fix #10: _sendMessage() now builds history from _messages list (all msgs
      except the just-added current one) and passes to ApiService.sendMessage().
    - Fix #16: Drawer "New Category" tile → "Manage Categories" (Icons.category).
      New _ManageCategoriesSheet widget: lists all categories with per-item
      delete buttons + "New Category" FilledButton at bottom that opens the
      existing _NewCategorySheet. Delete goes through ApiService.deleteCategory().

  lib/screens/category_screen.dart + pubspec.yaml:
    - Fix #13: Added file_picker ^8.0.0 dependency.
      New _importCsv(fields) method: opens file picker for .csv files,
      decodes UTF-8, parses quoted CSV (handles commas/quotes inside fields),
      matches headers to field display_name or field_name (case-insensitive),
      upserts each row via SupabaseService.upsertItem(), shows snackbar with count.
      Added upload IconButton (Icons.upload) to AppBar beside existing share button.
      Both import and export share one FutureBuilder wrapping both buttons.

================================================================================
 RECENT CHANGES (2026-03-03)
================================================================================

  settings_screen.dart — Added Data tab (3rd tab) + Delete Account + refactor:

    - TabController length: 2 → 3
    - Tab 3 "Data": lists all user categories, each with Import CSV (file_picker)
      and Export CSV (share_plus) buttons. Import matches headers by display_name
      or field_name (case-insensitive), upserts rows, shows imported count snackbar.
      Export builds CSV with proper quoting, shares via native share sheet.
    - _ProfileTab: added "Danger Zone" section below Sign Out with Delete Account
      button (OutlinedButton, error color). Tap shows confirm AlertDialog; on
      confirm calls ApiService.deleteAccount(), signs out, navigates to LoginScreen.
    - _ProfileTab: now receives onDeleteAccount VoidCallback.
    - _SettingsScreenState: added ApiService instance + _deleteAccount() method.

  lib/services/api_service.dart:
    - New deleteAccount() method: refreshes session, calls edge function in
      direct tool_call mode with run_sql → DELETE FROM auth.users WHERE id = userId.
      Cascades to profiles and all user data via ON DELETE CASCADE.

================================================================================
 RECENT CHANGES (2026-03-03, session 2)
================================================================================

  lib/screens/chat_screen.dart — Drawer header redesign (item #17):
    - Replaced DrawerHeader widget (plain primaryContainer banner) with a compact
      custom gradient Container (primary → tertiary blend, 35% lerp).
    - Shows app logo emoji + name on left, user email below in white/75% opacity.
    - Uses MediaQuery top padding so it sits correctly under the status bar.
    - No more big blank "Stockai" banner — clean, compact, themed.

  lib/main.dart — Background tint increase (item #3):
    - Light mode surface tints bumped slightly (surface: 5%→8%, others +2-3%)
      so the chosen theme color is more visibly present in the background
      while Material 3 onSurface/onPrimary colors guarantee text contrast.

  lib/screens/settings_screen.dart — Theme tab + Data tab (items #3, #16):
    - "Appearance" tab renamed → "Theme".
    - _AppearanceTab renamed → _ThemeTab.
    - Added "Custom Color" section at bottom of Theme tab:
        _HexColorInput widget: color swatch preview + hex text field + Apply button.
        Shows current seed color as hint. Parses 6-char hex (with or without #).
        Apply button enabled only when a valid hex is typed; saves via setUiSetting.
    - Data tab: added category management alongside import/export.
        "New" TextButton.icon in header opens _DataCategorySheet bottom sheet
        (name field + emoji picker, same layout as chat screen's _NewCategorySheet).
        Creates via ApiService.sendMessage() natural language call, reloads on success.
        Each category row now has 3 icon buttons: Import CSV, Export CSV, Delete.
        Delete shows confirm dialog then calls ApiService.deleteCategory(), reloads.

  Item #4 confirmed (no code change needed):
    - Web AI controls theme via set_ui_setting tool in smart-api-v4.ts ✓
    - App AI: loadThemeFromSupabase() called after every AI response in
      _sendMessage() (chat_screen.dart line ~119) ✓ — theme changes from AI apply live.

================================================================================
 RECENT CHANGES (2026-03-03, session 3)
================================================================================

  pubspec.yaml — new packages:
    flutter_colorpicker: ^1.1.0, excel: ^4.0.2, path_provider: ^2.1.0
    Note: excel 4.0.6 was resolved; TextCellValue.value is TextSpan (not String).

  lib/models/theme_settings.dart:
    - Added CustomPreset class (name, seedColor, customColors Map<String,Color>)
      with toJson()/fromJson() for Supabase persistence.
    - ThemeSettings gained customColors: Map<String, Color> field (keys: background,
      cards, accent). These override specific scheme roles in _buildTheme().

  lib/main.dart:
    - Added customPresetsNotifier: ValueNotifier<List<CustomPreset>>([])
    - loadThemeFromSupabase() now also reads bg_color, card_color, accent_color,
      custom_presets (JSON array) from ui_settings.
    - _buildTheme() now accepts Map<String,Color> customColors. Applies:
        background → surface + surfaceContainerLow variants
        cards → surfaceContainer + surfaceContainerHigh variants
        accent → secondary + secondaryContainer + tertiary
    - InventoryManagerApp passes settings.customColors to _buildTheme().

  lib/screens/settings_screen.dart — complete Theme + Data tab rewrite:

    Theme tab (now StatefulWidget):
    - Color wheel picker (HueRingPicker from flutter_colorpicker) opened in a
      dialog via StatefulBuilder for real-time preview. Replaces hex text input.
    - Per-element color rows: Primary, Background, Cards, Accent.
      Each shows current color swatch + hex or "Auto from theme" label.
      Optional reset button appears when a custom override is set.
    - Built-in presets grid unchanged (6 presets).
    - Custom presets grid: appears when presets are saved; each has an X delete
      button. Tapping a custom preset loads it (seedColor + all customColors).
    - "Save as Preset" section: name text field + Save button saves current
      seedColor + customColors as a CustomPreset, persists to ui_settings as JSON.
    - All theme writes call setUiSetting() directly from within the tab.

    Data tab (split into two sections):
    - Section 1 (top): Categories Management
        Create/delete categories + per-row export (CSV via Share.share).
        Import button removed from per-row; export only per row now.
    - Section 2 (bottom, after Divider): Import & Export
        "Export All" button: collects all categories → builds xlsx (one sheet per
        category) using excel package → writes to temp dir → shares via
        Share.shareXFiles. Sheet names match category display names.
        "Import" button: FilePicker (csv/xlsx). For xlsx: matches sheet names to
        categories, imports rows. For csv: shows category selection dialog first.
        Accepts Google Sheets exports (.xlsx format).
    - Note: excel package 4.x exports Border which conflicts with Flutter's Border.
      Fixed with: import 'package:excel/excel.dart' hide Border;

  APK: version 1.1.2 (4), 55.3MB
    Location: app/build/app/outputs/flutter-apk/app-release.apk
    Build tools: 35.0.0 (36.0.0 had broken install; downgraded in build.gradle.kts)

  AI theme fix (chat_screen.dart):
    - loadThemeFromSupabase() now awaited after AI response — theme updates instantly
    - Previously fired-and-forgot, causing race condition with UI render

  smart-api-v4.ts — AI theme awareness:
    - Added theme_color (Flutter seed/primary color hex) and theme_mode (light/dark/system)
      to set_ui_setting tool description and system prompt APPEARANCE section
    - AI now knows to use these keys to change the Flutter app's full palette and mode

  Netlify (Web):
    Site:     cchi.netlify.app
    Site ID:  a8646de0-6f9f-4b01-a211-0ea3ea9b394a
    Team ID:  6903b820c3307337b61b87ee
    Deploy:   netlify deploy --prod (from project root, publish dir = ".")
    CLI:      netlify-cli installed globally via npm
    Config:   netlify.toml in project root

  Firebase App Distribution:
    Project ID:    stockai-75833731-f9741
    Android App ID: 1:494810929579:android:798b5dc63ed5efba231aee
    Latest release: v1.1.2 (4) — distributed to Yjturetsky@gmail.com
    Tester email:  Yjturetsky@gmail.com (always include --testers flag when distributing)
    Deploy script: scripts/distribute.bat (Windows; run after: firebase login)
    Linux deploy:  firebase appdistribution:distribute app/build/app/outputs/flutter-apk/app-release.apk --app 1:494810929579:android:798b5dc63ed5efba231aee --project stockai-75833731-f9741 --testers "Yjturetsky@gmail.com" --release-notes "..."
    CLI:           firebase-tools 15.13.0 (global)
    google-services.json: app/android/app/

================================================================================
 FUTURE IDEAS
================================================================================

  - Email confirmation flow (currently auto-confirms)
  - Shared categories (invite other users to a category)
  - Dashboard charts / analytics
  - Push notifications for low stock
  - Flutter: add category browsing, item management (not just AI chat)
  - PWA support
  - Image uploads for items
  - Barcode scanner integration
  - ~~Remove run_sql tool from AI access~~ DONE (see Recent Changes)


INTERVIEW PREP CLEANUP (2026-04-20)
-----------------------------------
Codebase pass ahead of a job interview — priority was clarity, removing
redundant/dead code, and consolidating duplication. Every change below
is web-only; backend (Edge Function + SQL) and Flutter were left alone.

  1. Supabase credentials — single source of truth
     Previously, each file that needed the URL/key had a
     `window.SUPABASE_URL || 'https://...hardcoded...'` fallback. The
     fallback was a copy-paste of config.js's value, so "change project =
     edit N files." Stripped the fallbacks from:
       - web/landing.html   (was line ~353)
       - web/index.html     (was line ~688)
       - web/inventory.html (was line ~319)
       - web/settings.html  (was line ~370)
       - web/auth.js        (was lines 17-18)
       - web/sidebar.js     (was line ~971)
       - web/ai-assistant-v4.js (was line 15)
     Now each reads strictly from window.SUPABASE_URL /
     window.SUPABASE_ANON_KEY set by config.js. One place to change.

  2. Fixed missing config.js in about.html
     about.html was loading auth.js + sidebar.js without loading
     config.js first — the fallbacks above were quietly masking this.
     Added <script src="/config.js"></script> before the supabase lib.

  3. Removed dead `if (!window.db)` guard in auth.js
     The guard could never fire — auth.js is the only place that sets
     window.db, and it only runs once per page. Collapsed 3 lines to 1.

  4. Removed web-side app_name feature from sidebar.js
     The "user can rename the app" feature didn't make sense in a
     multi-tenant product. sidebar.js no longer reads theme.app_name
     or extracts the legacy 📦 emoji — always renders "StockAI" + the
     SVG cube logo. Related backend/Flutter cleanup is below in
     CLEANUP DEBT.

  5. Switched `auth-ready` from event-only to Promise-based
     auth.js now exposes `window.authReady` as a Promise that only
     resolves on a successful auth. The 'auth-ready' CustomEvent is
     still dispatched for old-style listeners.
     Before: sidebar.js had a 3-line `if (!window.authReady) { await
     new Promise(...) }` gate.
     After:  `await window.authReady;` — one line, cleaner semantics.
     On the redirect path, the promise stays pending forever (the page
     is unloading anyway), so no explicit "if no user, bail" guard is
     needed in consumers.

  6. Extracted shared CSS to web/global.css (NEW FILE)
     Previously the body fade-in, CSS variable defaults, html
     font-size, and universal reset were duplicated across
     index/inventory/settings/landing/about.html AND sidebar.js — six
     copies of the same thing.
     Created web/global.css with the canonical shared rules. Each HTML
     now loads it via <link rel="stylesheet" href="/global.css"> and
     keeps ONLY its page-specific overrides inline. sidebar.js's
     injected <style> block also had its global chunks (body, html,
     body.loaded, --font-base) removed — only sidebar-scoped rules
     remain.
     Drift note: settings.html and about.html have --clr-bg: #f5f5f5
     (vs. #eef0f6 on dashboard/inventory). Left in place as inline
     overrides; worth unifying later.
     Follow-up: also removed redundant `<body style="opacity:0;
     transition:opacity 0.15s">` inline attributes from index.html,
     settings.html, and inventory.html — global.css now owns this.

  7. Updated sidebar.js file header
     Now states explicitly that sidebar.js owns both the desktop
     sidebar and the mobile topbar (hamburger + title), since they
     share state and wiring.

  8. Tightened helper function docstrings in sidebar.js
     The single-line /** */ comments above each helper now describe
     what the function returns AND where it's used (e.g., "used by the
     mobile topbar", "used for the avatar circle"). Old version was
     vague ("Get initials from a name").

  9. Reorganized sidebar.js section numbering
     Before: sections 1, 2, 3, 6, 7, 8, 9, 10, 11, 11, (sections 4/5
     had been deleted in earlier refactors, and a duplicate 11 had
     snuck in at the bottom).
     Also: "7. HELPER FUNCTIONS" lumped together the three small
     helpers (getInitials, getCurrentPageName, isNavItemActive — each
     under 20 lines) with buildSidebar (118 lines). buildSidebar isn't
     a helper, it's the main builder.
     After: clean 1-11 with no gaps or dupes, AND reordered so every
     section comes after everything it depends on (no forward refs):
        1. APPLY THEME
        2. WAIT FOR AUTH
        3. LOAD CATEGORIES
        4. INJECT STYLES
        5. HELPER FUNCTIONS         (getInitials, getCurrentPageName,
                                     isNavItemActive)
        6. BUILDERS                 (buildSidebar, buildMobileTopbar,
                                     buildOverlay)
        7. GLOBAL FUNCTIONS         (window.toggleSidebar, sidebarLogout,
                                     refreshSidebarCategories)
        8. FEEDBACK MODAL FUNCTIONS (openFeedbackModal, closeFeedbackModal,
                                     submitFeedback)
        9. INJECT SIDEBAR INTO DOM  (uses everything above)
       10. SHOW PAGE
       11. DISPATCH READY EVENT
     Before the reorder, Section 7 (inject) came before Sections 8 and 9
     (definitions) — it worked only thanks to function hoisting. Now
     reading top-to-bottom follows the actual dependency order.

 10. auth.js — header comment rewritten for accuracy
     Old comment said "Must be loaded before sidebar.js on every page."
     That was out of date — multiple consumers now wait on auth (sidebar.js,
     ai-assistant-v4.js, inline scripts in each HTML). The new comment
     describes the actual load-order contract and the `window.authReady`
     Promise semantics (resolves only on success; stays pending on the
     redirect path so consumers hang harmlessly while the page unloads).
     File structure (5 numbered sections) was already clean — no code move.

 11. Extracted shared nav-build logic in sidebar.js → buildNav()
     The nav (Dashboard + categories + Settings + Feedback + About) was
     constructed in TWO places:
       - buildSidebar()               — initial render on page load
       - refreshSidebarCategories()   — rebuild after a category is
                                        created/renamed/deleted
     ~100 lines of the same code, with small drift (the refresh path
     was missing the About link that buildSidebar had).
     Extracted into a new helper `buildNav()` in section 6 (BUILDERS).
     Both callers now do `buildNav()` instead of constructing inline.
     Net: sidebar.js dropped from ~1029 to ~926 lines, and the missing
     About link is now present after a refresh.

 12. Renamed jargon-heavy section headers in sidebar.js
     "INJECT STYLES"          → "ADD SIDEBAR STYLES"
     "INJECT SIDEBAR INTO DOM" → "ADD SIDEBAR TO PAGE"
     "DISPATCH READY EVENT"    → "SIGNAL SIDEBAR IS READY"
     "Inject" and "dispatch" are accurate terms but read as jargon to
     anyone not already steeped in DOM-speak. The new names describe
     what the section does in plain terms.

 13. Extracted shared display-name fallback → window.displayNameFrom
     The fallback `email.split('@')[0] || 'User'` pattern was inline in
     two places: auth.js (when fabricating a profile after a failed
     fetch) and sidebar.js (when rendering the footer displayName).
     Extracted as a one-liner in auth.js:
       window.displayNameFrom = (email) => email?.split('@')[0] || 'User';
     Both consumers now call it. Future changes to the fallback logic
     (e.g. capitalize, strip numbers) happen in one place.

 14. Unified the "something changed" event system → data-changed
     Before the cleanup there were THREE different refresh triggers,
     each with their own listeners (or no listeners at all):
       - `window.refreshSidebarCategories()` direct call — fired from
          settings.html on category delete. sidebar.js only.
       - `categories-updated` CustomEvent — fired from index.html after
          a new category was created. **Nobody listened for it.**
       - `ai-refresh` CustomEvent — fired from ai-assistant-v4.js after
          any AI tool call. Listened by index.html and inventory.html,
          but NOT by sidebar.js (so AI-driven category changes never
          updated the nav).
     The gaps meant creating a category from the dashboard, or letting
     the AI create one via chat, required a full page reload before
     the sidebar nav showed it.
     After: one event — `data-changed` — fired from every mutation
     site (settings.html, index.html, ai-assistant-v4.js) and listened
     by everyone that renders changeable data (sidebar.js, index.html,
     inventory.html). Bonus: ai-assistant-v4.js's triggerRefresh went
     from 8 lines (event + 2 direct function calls to specific pages)
     down to 3 — the event reaches every listener.

 15. Extracted shared category-load helper → loadCategories()
     Section 3 (initial page load) and refreshSidebarCategories (later
     refresh) had near-identical DB queries for this user's
     categories. Extracted into `loadCategories()` in Section 5.
     Both callers now just do `await loadCategories();`.
     Section 3 collapsed from 12 lines to 4. refreshSidebarCategories
     collapsed from 21 lines to 12. Single source of truth for "how
     we fetch categories."

 16. Section 10 of sidebar.js renamed "SHOW PAGE (auth complete)"
     → "SHOW PAGE"
     The "(auth complete)" parenthetical was a stale note from when
     sidebar.js handled auth itself. Auth was split into auth.js (see
     entry 5), so the note no longer tells the truth about this file.
     Also removed the redundant `document.body.style.opacity = '1'`
     line — it was a fallback for when body had inline
     `style="opacity:0"`, which we stripped earlier when global.css
     took over the fade-in. Section 10 is now a single line:
       document.body.classList.add('loaded');

 17. Numbered index.html's banner comments for parity with sidebar.js
     Before: banner comments were named (SETUP, INSTANT THEME, CSS
     STYLES, BODY, SCRIPT LOADING ORDER, DASHBOARD SCRIPT, AI CHAT
     BUBBLE) but unnumbered. After: numbered 1-7 in load order, same
     convention as sidebar.js's internal sections. Makes it easier to
     reference "section N of index.html" in discussion.

 18. Added numbered banner comments to landing.html
     Landing had no section banners previously — just opens straight
     into <!DOCTYPE html>, then head/body with no signposts. Added
     the same numbered-banner convention index.html uses:
        1. SETUP              (DOCTYPE + head meta)
        2. HEAD DEPENDENCIES  (config.js + Supabase lib + global.css)
        3. CSS STYLES         (landing-specific <style> block)
        4. BODY               (login / signup / reset UI)
        5. AUTH SCRIPT        (login, signup, password reset handlers)

 19. Fixed invisible-landing-page bug
     Discovered while walking through landing.html's flow. After entry
     6 (body fade-in moved to global.css) and the later cleanup that
     removed the inline `<body style="opacity:0">` from HTML files,
     landing had no way to reveal itself: global.css sets
     `body { opacity: 0 }` and `body.loaded { opacity: 1 }`, but
     landing doesn't load sidebar.js (which is what adds the `loaded`
     class on other pages), and nothing in landing's own script adds
     it either. Result: the login page was rendering at opacity 0
     after the cleanup — completely invisible.
     Fix: set `<body class="loaded">` inline on landing.html. Landing
     has no async setup to hide (unlike the post-auth pages), so it
     can be visible immediately — no fade-in gate needed.

 20. Numbered + cleaned up ai-assistant-v4.js
     a) Banner style: the file had dash-box mini-sections
        (`/* ── Name ──── */`). Converted to the numbered-banner style
        used by sidebar.js:
          1. SETUP + STATE
          2. AUTH HELPERS
          3. CONTEXT HELPERS
          4. REFRESH HOOKS
          5. WIDGET CREATION
          6. MESSAGE RENDERING
          7. SEND MESSAGE
          8. PUBLIC API
          9. STYLES
         10. INIT
     b) Removed stale "KEY CHANGE vs v3" docstring on getAuthHeaders.
        v3 was replaced long ago; the comparison note is dead weight.
     c) Simplified init from 11 lines to 4. Before it tried to init
        three ways (immediate, DOMContentLoaded, sidebar-ready); the
        first two always failed the auth check and returned without
        doing anything, because auth isn't ready that early. Only
        sidebar-ready actually created the widget. Now only sidebar-
        ready is used.
     d) Simplified reply extraction in sendMessage from 8 lines to 1.
        It had three fallback branches trying data.content[].text,
        then data.message, then data.text. But the Edge Function
        always returns `data.message` in both AI-mode and direct
        tool_call mode responses — so only that one line is needed:
          const reply = data.message || '(No response)';
        The other branches were defending against a shape the backend
        never actually returns.
     File total: 569 → 547 lines.

 21. Removed the dead `ai_panel_size` / setAIPanelSize feature
     Discovered while walking through Section 8 of ai-assistant-v4.js:
     the `window.setAIPanelSize` function was defined and on window,
     and the backend accepted `ai_panel_size` as a `set_ui_setting`
     parameter — but nothing ever called setAIPanelSize, and nothing
     read the setting back from the DB on page load. Half-built
     feature. User saying "make the chat bigger" would silently save
     to the DB but the panel never resized.
     Stripped:
       - web/ai-assistant-v4.js — removed AI_PANEL_SIZES lookup,
         window.setAIPanelSize function, CSS variables for panel
         dims, and the old Section 8 "PUBLIC API". Renumbered
         Section 10 INIT → 9. File now 547 → 526 lines.
       - backend/smart-api-v4.ts — removed ai_panel_size from the AI
         tool description (line 257) and the allowed set_ui_setting
         columns (line 926).
       - web/ai-assistant-v4.js header comment — stripped the stale
         "changes from v3" history; replaced with a plain description
         of current behavior.
     Still in the duplicate Edge Function at
     supabase/functions/smart-api/index.ts (see CLEANUP DEBT below) —
     strip it there when the duplicate is resolved.

 22. Backend quick-pass — smart-api-v4.ts + SQL seed
     a) Added numbered banners to backend/smart-api-v4.ts:
          1. TOOL DEFINITIONS
          2. HELPERS
          3. TOOL EXECUTOR
          4. MAIN REQUEST HANDLER
        Same convention as the web files. Sub-section dash-headers
        inside sections 3 and 4 (READ / CATEGORIES / FIELDS / ITEMS /
        UI SETTINGS, and Token-extraction / clients / validation /
        etc.) left in place as useful landmarks.
     b) Stripped the last `app_name` references (Scope B completion):
          - backend/smart-api-v4.ts: removed from AI tool description
            (line 204) AND from allowed set_ui_setting columns
            (line 920).
          - backend/inventory-setup-v4.sql: removed the INSERT row
            that seeded '📦 Inventory Manager' for every new user.
        The duplicate Edge Function at supabase/functions/smart-api/
        index.ts still contains both references — that duplicate is
        Scope B's other shoe, to be resolved when we pick a canonical
        file.

 23. config.js left as two lines (no diagnostic check)
     Briefly added a "credentials missing" console.error guard, then
     realized the two lines above it are hardcoded string literals —
     the guard could never fire. Stripped back to the minimal form.

CLEANUP DEBT (2026-04-20)
-------------------------
TODO: Finish removing the `app_name` customization feature (Scope B + C).
  The web side was stripped — sidebar.js no longer reads `theme.app_name`
  and always renders "StockAI" + the default SVG logo. The feature is still
  alive in other places:

    - backend/inventory-setup-v4.sql:224
        Still seeds every new user's ui_settings with
        '📦 Inventory Manager' — remove this INSERT row.

    - backend/smart-api-v4.ts:205, 922
        The AI tool description still lists `app_name` as a settable key,
        and line 922 includes it in the allowed set_ui_setting columns.
        The AI can still "succeed" at setting it — it just does nothing
        visible. Remove from both places and redeploy the Edge Function.

    - supabase/functions/smart-api/index.ts
        Duplicate of the Edge Function. Either this or backend/smart-api-v4.ts
        is stale. Decide which is canonical and delete the other.

    - app/lib/screens/chat_screen.dart:263-275
        Flutter still reads app_name + extracts emoji. Replace with the
        hardcoded "StockAI" title and the app's SVG/branded logo widget.

  Reason to finish this: currently a user can "rename the app" through
  the AI chat, and the write silently succeeds but nothing renders it.
  That's a hidden no-op — worse than a clear error. Finishing Scope B/C
  removes the feature surface entirely.

  Also on the debt list:
    - Duplicate Edge Function path (backend/ vs supabase/functions/) —
      resolve.

================================================================================
