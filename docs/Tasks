🛠️ StockAI Development Plan (v2.1 - Refined)
1. Visual Refresh & Branding
[ ] Side Panel Update: Update the mobile app’s side panel top header. Replace old assets/text with the new visual identity (Logo + "StockAI" name).

[ ] App Icon: Replace the current launcher icon with the new geometric cube logo.

[ ] Keyboard Focus: Configure the app to automatically open with the keyboard active and focus set on the AI Chat text field upon launch.

2. Emergency API & Logic Fixes
[ ] API Debugging: Investigate and fix the current API error (likely a regression from the recycling_bin table implementation).

[ ] Recycling Bin (Backend): Confirm soft_delete logic is moving items to recycling_bin.

3. Category & Inventory Logic ("The Anchor Duo")
[ ] The "Immutable Mapping" Rule: Even if a user renames "Name" to "Item" or "Quantity" to "Stock," the app must visually and programmatically mark these as the Primary Identity and Primary Count.

The UI must show a "Required" or "System" indicator so the user always knows they are editing the core fields.

Name must always be 1st; Quantity must always be 2nd.

[ ] App Layout Optimization: Use DevTools to fix the "Subtitle Blur." Instead of dumping extra fields into a messy subtitle, create a clear, labeled grid so users know exactly what data they are looking at.

[ ] Cross-Screen Audit: Review the "Edit" fields on all secondary screens. Some layouts are currently "off" or misaligned—realign them to match the new high-contrast clean design. (Refer to the recent screenshots for the "off" alignment).

4. ElevenLabs TTS Integration (Partial Freeze)
[ ] Real-Time Params: Capture stability and similarity_boost values at the exact moment of the "Play" click.

[ ] Stream Endpoint: Prepare transition to /v1/text-to-speech/{voice_id}/stream.

5. Mobile Web Optimization
[ ] Data Tab Cleanup: Redesign "Data" tab buttons. Fix the "dumped together" look on mobile—use proper vertical spacing and grouping.

[ ] AI Chat UI Fix: * Resize the AI Chat icon (too large).

Move icon so it no longer blocks the Send button.

Adjust top padding so the Downward Arrow (minimize) is visible on all mobile aspect ratios.

6. Backend: AI Recycling Bin
[ ] Soft Delete: Ensure deleted items hit the recycling_bin table with a 30-day TTL (Time-To-Live).