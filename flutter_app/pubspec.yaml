name: inventory_manager
description: Stockai - AI-powered inventory management
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependency_overrides:
  app_links: ^7.0.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: '>=2.3.0'
  http: '>=1.2.0'
  speech_to_text: '>=7.0.0'
  flutter_tts: '>=4.0.0'
  share_plus: '>=10.0.0'
  file_picker: '>=8.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '>=6.0.0'

flutter:
  uses-material-design: true
